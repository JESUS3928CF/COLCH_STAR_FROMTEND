const GuardarModal = ({onClick}) => {
  return (
      <input
          type='submit'
          className='btn btn-success'
          value='Guardar'
          onClick={onClick}
      />
  );
}


export default GuardarModal
